export const metadata = {
  title: 'Vanilla Tilt Card',
  description:
    'Vanilla Tilt Card is a card component that uses the Vanilla Tilt library to create a 3D tilt effect.',
  alternates: {
    canonical: '/vanilla-tilt-card',
  },
};

import { ComponentPreview } from '@/components/ComponentPreview';
import { VanillaTiltCardPreview } from './preview/vanilla-tilt-card';

# Vanilla Tilt Card

<span className="text-[15px]">
  The Vanilla Tilt Card component is a card component that uses the Vanilla Tilt
  library to create a 3D tilt effect.
</span>

<Container slug="vanilla-tilt-card">
  <ComponentPreview componentName="vanilla-tilt-card" className="h-full w-full">
    <VanillaTiltCardPreview />
  </ComponentPreview>
</Container>

## Usage

<span className="text-[15px]">
  Follow the steps below to add the Vanilla Tilt Card Component to your project.
</span>

{/* -----------------------------USAGE - STEP - 01 ---------------------------> */}

<UsageSteps stepNumber={1} title="Add the import statement for the component">
  ```tsx 
  import {VanillaTiltCard} from "@/components/elixir-ui/vanilla-tilt-card"; 
  ```
</UsageSteps>

{/* -----------------------------USAGE - STEP - 02 ---------------------------> */}

<UsageSteps
  stepNumber={2}
  title="Import the component in your project and use it"
>
  ```tsx
  <div className="flex h-full w-full items-center justify-center">
    <VanillaTiltCard className="flex w-fit flex-col">
      <Image
        src="/components/john-lennon.jpg"
        alt="John Lennon"
        width={1000}
        height={1000}
        className="aspect-auto h-60 w-60 rounded-lg border object-cover"
      />
      <h1 className="mt-2 rounded-lg bg-black p-2 text-center text-xl font-semibold text-white uppercase dark:bg-white/5 dark:text-white">
        John Lennon
      </h1>
    </VanillaTiltCard>
  </div>
  ```
</UsageSteps>

## Props

<MDXPropsTable
  props={[
    {
      prop: 'options',
      type: 'object',
      default: '-',
      description: 'The options for the Vanilla Tilt Card',
    },
    {
      prop: 'options.max',
      type: 'number',
      default: '5',
      description: 'The maximum tilt intensity',
    },
    {
      prop: 'options.speed',
      type: 'number',
      default: '300',
      description: 'The speed of the tilt',
    },
    {
      prop: 'options.glare',
      type: 'boolean',
      default: 'true',
      description: 'Whether to enable glare',
    },
    {
      prop: 'options.scale',
      type: 'number',
      default: '1.1',
      description: 'The scale of the tilt',
    },
    {
      prop: "options['max-glare']",
      type: 'number',
      default: '0.6',
      description: 'The maximum glare intensity',
    },
    {
      prop: 'children',
      type: 'React.ReactNode',
      default: '-',
      description: 'The children to be displayed',
    },
    {
      prop: 'className',
      type: 'string',
      default: '-',
      description: 'Additional CSS classes',
    },
  ]}
/>
