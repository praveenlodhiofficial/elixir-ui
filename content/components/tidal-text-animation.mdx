export const metadata = {
  title: 'Tidal Text Animation',
  description:
    'Team Carousel 02 showcases a tidal-text animation component is an interactive component where hovering over a team members image reveals their name rising from the bottom, mimicking a wave motion. In the default state, the team name is displayed',
  alternates: {
    canonical: '/tidal-text-animation',
  },
};

import { ComponentPreview } from '@/components/ComponentPreview';
import { TidalTextAnimationPreview } from './preview/tidal-text-animation';

# Tidal Text Animation

<span className="text-[15px]">
  The Tidal Text component is an interactive component where hovering over a
  team members image reveals their name rising from the bottom, mimicking a wave
  motion. In the default state, the team name is displayed.
</span>

<Container slug="tidal-text-animation">
  <ComponentPreview
    componentName="tidal-text-animation"
    className="h-full w-full"
  >
    <TidalTextAnimationPreview />
  </ComponentPreview>
</Container>

## Usage

<span className="text-[15px]">
  Follow the steps below to add the Tidal Text Animation Component to your
  project.
</span>

{/* -----------------------------USAGE - STEP - 01 ---------------------------> */}

<UsageSteps stepNumber={1} title="Add the import statement for the component">
  ```tsx 
  import { TidalTextAnimation } from "@/components/elixir-ui/tidal-text-animation"; 
  ```
</UsageSteps>

{/* -----------------------------USAGE - STEP - 02 ---------------------------> */}

<UsageSteps stepNumber={2} title="Add the team members props to the component">
  ```tsx
  const teamMembers = [
    { name: 'John Lennon', url: '/components/john-lennon.jpg' },
    { name: 'McCartney', url: '/components/paul-mccartney.jpg' },
    { name: 'Harrison', url: '/components/george-harrison.jpg' },
    { name: 'Ringo Starr', url: '/components/ringo-starr.jpg' },
  ]
  ```
</UsageSteps>

{/* -----------------------------USAGE - STEP - 02 ---------------------------> */}

<UsageSteps
  stepNumber={3}
  title="Import the component in your project and use it"
>
  ```tsx
  <div className="flex h-full w-full items-center justify-center">
    <TidalTextAnimation defaultText="The Beatles" teamMembers={teamMembers} />
  </div>
  ```
</UsageSteps>

## Props

<MDXPropsTable
  props={[
    {
      prop: 'defaultText',
      type: 'string',
      default: '-',
      description: 'The text to be displayed',
    },
    {
      prop: 'teamMembers',
      type: 'string',
      default: '-',
      description: 'The team members to be displayed',
    },
    {
      prop: 'className',
      type: 'string',
      default: '-',
      description: 'Additional CSS classes',
    },
  ]}
/>
